# Hook Examples

This directory contains optional hook examples for agents that support lifecycle hooks.

Do not read or apply these files during normal startup. Use them only when the user explicitly asks to configure hooks or deterministic automation.
